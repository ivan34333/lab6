#include <iostream>
#include "math.h"

using namespace std;

int main()
{
	int x,a;
	x > 999;
	cout << "Input number" << endl;
	cin >> x;
	a = x / 1000;
	(a > 10) || (a < 10) ? (a % 10) : (a / 1);
	cout << a;
	

}